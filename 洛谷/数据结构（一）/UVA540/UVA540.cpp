#include <cstdio>
#include <algorithm>
#include <deque>
using namespace std;

int t;
int s;

int main() {
    while(scanf("%d", &t) != 0) {
        printf("Scenario #%d\n", s++);
        for(int i = 0; i < t; i++) {
            int num;
            scanf("%d", &num);
            for(int j = 0; j < num; j++) {

            }
        }
    }
}